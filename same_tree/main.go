package same_tree

import (
		"leetcode/utils"
)

/* Assumes that:
The number of nodes in both trees is in the range [0, 100].
-10^4 <= Node.val <= 10^4 and Node.val != 0. */
func IsSameTree(p *utils.TreeNode, q *utils.TreeNode) bool {
		return utils.IsSameTree( p, q )
}
